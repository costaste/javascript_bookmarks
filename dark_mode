javascript:(() => {const dm = window.dm ? Math.abs(1 - window.dm) : 1;document.querySelector('html').style.filter = `invert(${dm})`;document.querySelectorAll('img, video').forEach((i) => i.style.filter = `invert(${dm})`);window.dm = dm;})();
